#include <math.h>

namespace Testing
{
	class MyProject
	{
	public:
		int Multiply(int x, int y) { return x * y; }
		double SquareRoot(double v) { return sqrt(v); }
		float AddDouble(float d1, float d2) { return d1 + d2; }
	};
}